import { StyleSheet } from "react-native";
import Colors from "@/constants/colors";
import type { ResponsiveUtils } from "@/lib/responsive";

export function createStyles(r: ResponsiveUtils) {
  return StyleSheet.create({
    container: {
      flex: 1,
      backgroundColor: Colors.background,
    },
    loadingContainer: {
      flex: 1,
      alignItems: "center",
      justifyContent: "center",
      backgroundColor: "#000000",
      gap: 20,
    },
    loadingLogo: {
      width: r.rs(120),
      height: r.rs(120),
    },
    loadingText: {
      fontSize: r.rf(16),
      fontFamily: "Inter_500Medium",
      color: "#FFFFFF",
      opacity: 0.7,
    },
    map: {
      flex: 1,
    },
    menuBtn: {
      position: "absolute",
      left: 16,
      width: Math.max(r.rs(44), 44),
      height: Math.max(r.rs(44), 44),
      borderRadius: Math.max(r.rs(22), 22),
      backgroundColor: Colors.surface,
      alignItems: "center",
      justifyContent: "center",
      shadowColor: "#000",
      shadowOffset: { width: 0, height: 2 },
      shadowOpacity: 0.12,
      shadowRadius: 6,
      elevation: 4,
    },
    stepCounterCard: {
      position: "absolute",
      right: 16,
      minWidth: r.rs(92),
      flexDirection: "row",
      alignItems: "center",
      gap: 10,
      paddingVertical: 8,
      paddingHorizontal: 10,
      borderRadius: 18,
      backgroundColor: Colors.mapOverlay,
      shadowColor: "#000",
      shadowOffset: { width: 0, height: 2 },
      shadowOpacity: 0.12,
      shadowRadius: 6,
      elevation: 4,
    },
    stepCounterIcon: {
      width: r.rs(32),
      height: r.rs(32),
      borderRadius: r.rs(16),
      backgroundColor: Colors.primaryLight,
      alignItems: "center",
      justifyContent: "center",
    },
    stepCounterContent: {
      minWidth: 0,
    },
    stepCounterValue: {
      fontSize: r.rf(15),
      fontFamily: "Inter_700Bold",
      color: Colors.text,
    },
    stepCounterLabel: {
      fontSize: r.rf(11),
      fontFamily: "Inter_500Medium",
      color: Colors.textSecondary,
      textTransform: "uppercase",
      letterSpacing: 0.6,
    },
    bottomControls: {
      position: "absolute",
      bottom: 0,
      left: 0,
      right: 0,
      paddingHorizontal: r.rs(16),
      gap: 8,
      maxHeight: "48%",
      zIndex: 20,
    },
    bottomControlRow: {
      flexDirection: "row",
      alignItems: "center",
      gap: 8,
    },
    dashboardContainer: {
      width: "100%",
    },
    emergencyBtn: {
      width: Math.max(r.rs(52), 44),
      height: Math.max(r.rs(52), 44),
      borderRadius: r.rs(18),
      backgroundColor: Colors.mapOverlay,
      borderWidth: 1,
      borderColor: Colors.border,
      alignItems: "center",
      justifyContent: "center",
      shadowColor: "#000",
      shadowOffset: { width: 0, height: 3 },
      shadowOpacity: 0.2,
      shadowRadius: 6,
      elevation: 5,
    },
    searchContainer: {
      flex: 1,
      minWidth: 0,
    },
    reportBtn: {
      width: Math.max(r.rs(52), 44),
      height: Math.max(r.rs(52), 44),
      borderRadius: r.rs(18),
      backgroundColor: Colors.mapOverlay,
      borderWidth: 1,
      borderColor: Colors.border,
      alignItems: "center",
      justifyContent: "center",
      shadowColor: "#000",
      shadowOffset: { width: 0, height: 3 },
      shadowOpacity: 0.2,
      shadowRadius: 6,
      elevation: 5,
    },
  });
}
